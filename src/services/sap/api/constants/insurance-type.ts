/**
 * Currently, Completed only use regular package which is INS01.
 * Package must have value over 500K IDR to use insurance
 */
const InsuranceType = {
  REGULAR_PACKAGE : "INS01",
  PRECIOUS_DOCUMENT: "INS02"
}

export default InsuranceType;